server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    types {
        application/vnd.android.package-archive apk;
    }

    location / {
        try_files $uri /index.html;
    }
}
